<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <title><%= titre %></title>
    <%= render 'layouts/stylesheets' %>
  </head>
  <body>
    
    <div class="container">
      <header>
        <%= render 'layouts/head' %>
      </header>
      
      <section class="round">
        <%= yield %>
      </section>
        <%= render 'layouts/foot' %>
    </div>
    <%= debug(params) if Rails.env.development? %>    
  </body>
</html>