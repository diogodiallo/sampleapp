<!--<% @users.each do |user| %>-->
  <li>
    <%= gravatar_for user, :size => 30 %>
    <%= link_to user.nom, user %>
    <% if current_user.admin? %>
      <%= link_to 'Supprimer', user, :method => :delete, :confirm => 'Etes-vous sur?', :title => "Supprimer #{user.nom}" %>
    <% end %>
  </li>
<!--<% end %>-->